apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}    
      {{- end }}
      labels:
        app: {{ .Release.Name }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.schedule.affinity }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.schedule.topologySpreadConstraints }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.schedule.tolerations }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- if .Values.rbac.enabled }}
      serviceAccount: {{ .Release.Name }}
      {{- end }}
      {{- with .Values.initContainers}}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.volumes }}
      volumes:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- include "common-rule.dnsConfig" . | nindent 6 }}
      {{- include "common-rule.podSecurityContext" . | nindent 6 }}
      {{- include "common-rule.nodeNamespaceShareRestrict" . | nindent 6 }}
      containers:
      - image: {{ .Values.app.image }}
        imagePullPolicy: {{ .Values.app.imagePullPolicy }}
        name: {{ .Release.Name }}
        ports:
          - name: http
            protocol: TCP
            containerPort: {{ .Values.app.port }}
        {{- with .Values.app.customCommand }}
        command: 
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.app.customArgs }}
        args:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.app.env }}
        env:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.app.envFrom }}
        envFrom:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.probes.readiness }}
        readinessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.probes.liveness }}
        livenessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.volumeMounts }}
        volumeMounts:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        {{- include "common-rule.resources" . | nindent 8 }}
        {{- include "common-rule.containerSecurityContext" . | nindent 8 }}
        

